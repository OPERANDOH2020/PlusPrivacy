function Config(){
    return {
        swarmClient: {
            host: "localhost",
            port: 8080,
            tenant: "ANALYST-EXTENSION"
        },
        guest:{
            email:"guest@operando.eu",
            password:"guest",
            tenant:"GUEST"
        },
        SERVER_HOST_PROTOCOL:"http",
        WEBSITE_HOST:"localhost"
    };
};
