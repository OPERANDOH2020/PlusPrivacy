function Config(){
    return {
        swarmClient: {
            host: "localhost",
            port: 8080,
            tenant: "OSP-APP"
        },
        guest:{
            email:"guest@operando.eu",
            password:"guest",
            tenant:"GUEST"
        }
    };
};
